import SwiftUI

public struct SettingView: View {
    public init() {}
    
    public var body: some View {
        VStack {
            Text("Setting")
                .font(.largeTitle)
                .fontWeight(.bold)
        }
    }
}